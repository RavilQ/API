
@{
    ViewData["Title"] = "index";
    Layout = null;

    List<string> products = (List<string>)ViewBag.heroes;
}

<h1>index</h1>

<ul>
    @for (int i = 0; i < products.Count; i++)
    {
        <li>
            @products[i]
        </li>
    }
</ul>